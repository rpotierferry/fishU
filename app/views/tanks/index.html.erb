<div class="index">
<% if @user.tanks %>
  <% @tanks.each do |tank| %>
    <%= link_to tank.fish.last.name, tank_path(tank) %>
  <% end %>
  <br>
<% end %>
<%= link_to 'Creer un nouvel aquarium', new_tank_path %>
</div>
