<div class="navbar">
  <div class="logo">
      <%= image_tag 'fishu/logo.png', style:"width:80px"; %>
  </div>
</div>
<div class="screendisplay">
  <% if @user.tanks.empty? == false %>
    <% @tanks.each do |tank| %>

      <%= link_to tank_path(tank), class:" ligne select" do %>
        <span class="nb"><%=tank.liters%></span>

          litres |
            <span class="nb"><%=tank.fish.count%></span>
            <%if tank.fish.count > 1 %>
              <%if tank.fish.last.sick?%>
                poissons | statut: malades
              <%else%>
                poissons | statut: sains
              <%end%>
            <%else%>
              <%if tank.fish.last.sick?%>
                poisson | statut: malade
              <%else%>
                poisson | statut: sain
              <%end%>
            <%end%>


      <%end %>
    <% end %>
  <% end %>
</div>

<div class=centerfooter>
<%= form_with model: [ Tank.new] do |f| %>
<%= f.submit "Creer mon aquarium", class: "btn-index "%>
<% end %>
</div>
